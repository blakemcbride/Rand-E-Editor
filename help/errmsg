*** EDITOR ERROR MESSAGE:

    When you see this file, it means that e couldn't use the file
    you wanted, and had to put up something, so this is it.
    To see the reason that prevented the editor from using the file
    type
		   ENTER edit filename RETURN
